import styled from "styled-components";

export const StyledNextLink = styled.a`
  color: #fff;
  text-decoration: none;
  cursor: pointer;
  padding: 0.5rem 1rem;
  transition: all 0.1s linear;
  &:hover {
    cursor: pointer;
	border-bottom: 4px solid dodgerblue;
	font-size: 1.1rem;
	transition: all 0.1s linear;
  }
  
`
